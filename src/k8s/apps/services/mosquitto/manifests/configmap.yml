---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mosquitto-config
  namespace: services
data:
  mosquitto.conf: |
    # Mosquitto configuration
    listener 1883
    allow_anonymous true

    # Persistence
    persistence true
    persistence_location /mosquitto/data/

    # Logging
    log_dest stdout
    log_type all
