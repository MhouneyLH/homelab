---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mosquitto-mqtt-tls
  namespace: services
spec:
  secretName: mosquitto-mqtt-tls
  issuerRef:
    name: letsencrypt-prod-issuer
    kind: ClusterIssuer
  dnsNames:
    - mqtt.lucas-homelab-festung-der-finsternis.duckdns.org
